import styled from 'styled-components';

export const PosterDescContainer = styled.h3`
  font-size: 1.4rem;
  font-family: 'Play', sans-serif;
  font-weight: 400;
  margin-top: 4.7rem;
  line-height: 2.5rem;
`;
